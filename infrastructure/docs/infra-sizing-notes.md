# Infra Sizing Notes (Chameleon)

Track requested/limited CPU, memory, and optional GPU per workload.

For each service, capture:

- requests and limits from manifests
- observed usage (`kubectl top`)
- short right-sizing rationale
