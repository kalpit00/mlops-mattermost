apiVersion: v1
kind: ConfigMap
metadata:
  name: ingress-controller-plan
  namespace: platform
data:
  notes: |
    Placeholder file to track ingress controller choice for Chameleon.
    Planned controller: ingress-nginx (or cloud-compatible alternative).
    Replace with real controller manifests during deployment phase.
