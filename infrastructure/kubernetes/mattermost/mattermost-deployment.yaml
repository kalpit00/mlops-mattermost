apiVersion: apps/v1
kind: Deployment
metadata:
  name: mattermost
  namespace: mattermost
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mattermost
  template:
    metadata:
      labels:
        app: mattermost
    spec:
      containers:
        - name: mattermost
          image: mattermost/mattermost-team-edition:latest
          ports:
            - containerPort: 8065
          env:
            - name: MM_SQLSETTINGS_DRIVERNAME
              value: postgres
            - name: MM_SQLSETTINGS_DATASOURCE
              valueFrom:
                secretKeyRef:
                  name: mattermost-app-secret
                  key: datasource
            # Must match Mattermost Ingress host (nip.io encodes the Chameleon floating IP).
            - name: MM_SERVICESETTINGS_SITEURL
              value: "http://129-114-25-58.nip.io"
            - name: MM_SERVICESETTINGS_LISTENADDRESS
              value: ":8065"
            # Plain HTTP behind ingress (empty = no TLS at Mattermost)
            - name: MM_SERVICESETTINGS_CONNECTION_SECURITY
              value: ""
            # Let teammates open the URL and create accounts / join without admin invites (class/demo).
            - name: MM_TEAMSETTINGS_ENABLEUSERCREATION
              value: "true"
            - name: MM_TEAMSETTINGS_ENABLEOPENSERVER
              value: "true"
          readinessProbe:
            httpGet:
              path: /api/v4/system/ping
              port: 8065
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
          volumeMounts:
            - name: mattermost-data
              mountPath: /mattermost/data
      volumes:
        - name: mattermost-data
          persistentVolumeClaim:
            claimName: mattermost-data-pvc
