# Superseded by one file: use `infrastructure/.env.example` instead (copy to `infrastructure/.env`).
# This filename is kept for older notes; the same variables apply.
#
#   cp infrastructure/.env.example infrastructure/.env
#   set -a && source infrastructure/.env && set +a
#   ./infrastructure/scripts/create-secrets.sh
#   ./infrastructure/scripts/create-mlops-data-secrets.sh
#
# `infrastructure/secrets.env` remains gitignored if you still use that name locally.
