fastapi==0.115.8
uvicorn[standard]==0.34.0
pydantic==2.10.6
# Pin sklearn + joblib to exactly match training/requirements.txt so produced
# joblibs load cleanly in serving. Bump both files together when upgrading.
joblib==1.4.2
scikit-learn==1.5.2
